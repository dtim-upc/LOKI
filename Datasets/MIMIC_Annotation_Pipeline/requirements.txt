# Core data processing
pandas>=2.0
numpy>=1.24
tqdm>=4.65

# LLM provider SDKs (install the one(s) you need)
openai>=1.0
anthropic>=0.25
google-genai>=1.0

# HTTP client (used for LM Studio local provider)
requests>=2.31
